

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private Long id;


    @PrePersist
    void prePersist() {
        var now = LocalDateTime.now();
        if (expiration_date == null) expiration_date = now; // 최초 생성일
    }

    @NoArgsConstructor(access = AccessLevel.PROTECTED)